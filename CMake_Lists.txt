cmake_minimum_required(version 3.20)
set(CMAKE_LEGACY_CYGWIN_WIN32 OFF)

project(separate_files C CXX)

add_executable(main
    "main.cpp"
    "Actor.h"
    "Actor.cpp"
    "BoundingBox.h"
    "BoundingBox.cpp"
    "point.h"
    "point.cpp"
    "Vector2D.h"
    "Vector2D.cpp"
)

# We also could have done:
#set(SOURCES *.h *.cpp)
#add_executable(main ${SOURCES})

set_property(TARGET main PROPERTY CXX_STANDARD 20)